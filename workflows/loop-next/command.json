{
  "name": "loop-next",
  "description": "Loop the project's /next skill in fresh sub-agent contexts until PLAN.md has no unchecked boxes.",
  "script": "loop-next.js",
  "argKey": "maxSteps",
  "args": {},
  "argsSchema": {
    "type": "object",
    "properties": {
      "maxSteps": {
        "type": "integer",
        "minimum": 1,
        "maximum": 100,
        "default": 30,
        "description": "Iteration cap; the loop also stops when PLAN.md has no open boxes"
      }
    },
    "required": [],
    "additionalProperties": false
  }
}
