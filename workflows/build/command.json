{
  "name": "build",
  "description": "Plan a change, implement it item by item inside one git worktree, and review every item.",
  "script": "build.js",
  "argKey": "task",
  "argsSchema": {
    "type": "object",
    "properties": {
      "task": {
        "type": "string",
        "minLength": 1,
        "description": "What to build, in one sentence"
      },
      "context": {
        "type": "string",
        "description": "Repository background handed to every stage"
      },
      "maxItems": {
        "type": "integer",
        "minimum": 1,
        "maximum": 20,
        "default": 5,
        "description": "Plan items to produce; each one is implemented and reviewed on its own"
      },
      "maxFixes": {
        "type": "integer",
        "minimum": 0,
        "maximum": 3,
        "default": 1,
        "description": "Repair passes allowed per item after a 'changes' verdict"
      },
      "model": {
        "type": "string",
        "description": "Model for the plan and exec stages; defaults to the session model"
      },
      "reviewModel": {
        "type": "string",
        "description": "Model for the review stage; defaults to model, so set it for a second opinion"
      }
    },
    "required": ["task"],
    "additionalProperties": false
  }
}
