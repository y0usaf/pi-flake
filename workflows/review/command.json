{
  "name": "review",
  "description": "Review a diff in two independent contexts (correctness, risk), merge the findings, then optionally fix only the blocking ones. Usage: /review, or /review '{ \"scope\": \"main...HEAD\", \"mode\"?: \"offer fixes\" }'",
  "script": "review.js",
  "argKey": "scope",
  "questions": [
    {
      "key": "scope",
      "prompt": "review: which diff?",
      "options": ["working tree", "staged", "last commit", "vs main"],
      "free": true,
      "placeholder": "Any git diff revision range, e.g. main...HEAD"
    },
    {
      "key": "mode",
      "prompt": "review: what happens after the findings?",
      "options": ["review only", "offer fixes"]
    }
  ]
}
