{
  "name": "quick",
  "description": "Make one small change with a single agent: no plan, no review, no worktree.",
  "script": "quick.js",
  "argKey": "task",
  "argsSchema": {
    "type": "object",
    "properties": {
      "task": {
        "type": "string",
        "minLength": 1,
        "description": "The one small change to make, in one sentence"
      },
      "context": {
        "type": "string",
        "description": "Repository background handed to the agent"
      },
      "model": {
        "type": "string",
        "description": "Model for the single agent; defaults to the session model"
      }
    },
    "required": ["task"],
    "additionalProperties": false
  }
}
