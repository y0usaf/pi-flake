{
  "name": "pi-prime-tools",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Prime-agent-style flat status panels for Pi's builtin tool rows — header with queued/running/done/error state, animated spinner, tree rows, ascii symbol preset",
  "scripts": { "test": "bun test" },
  "pi": { "extensions": ["./src/index.ts"] },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "@sinclair/typebox": "*"
  }
}
